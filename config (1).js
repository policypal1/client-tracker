// Optional cloud sync. Leave both values blank to use local browser storage only.
// Supabase's anon key is designed to be used in browser apps when Row Level Security is enabled.
window.COMPLA_CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: ''
};
